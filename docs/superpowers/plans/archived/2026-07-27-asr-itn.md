# ASR 数字 ITN 实施计划

> **Status: ✅ 已完成**（Task 1-4 全部实现，commits `404f202b` / `654efdfe` / `de1b420e` / `a6ab36e0`）。2026-07-29 z-sync 回填 checkbox。
>
> **实现偏差**（Task 1 Step 3）：`itn::normalize` 实际实现远超 plan 示例的一行 `take_number_from_string`——加了「单数字保护（2+ 连续数字字符才转）+ 黑名单词 + 词边界匹配 + 占位符保护/还原」三层防护（后续 3 个 fix commit 迭代）。因此 `year_and_date` 测试断言从 plan 的 `"2026年7月26日"` 改为实际的 `"2026年七月26日"`（「七月」单数字被保护，符合「单数字+量词是地道中文不转」的设计取舍）。Task 2 的 ITN 接入点正确（corrector 后 hans 前），但抽成了 `postprocess_text` helper 被 `transcribe_batch` + `transcribe_segments_with_timestamps` 共用（DRY，行为等价）。
>
> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ASR 输出的中文数字（「二零二六」）自动转换为阿拉伯数字（「2026」），解决 Zipformer/Moonshine/Whisper 无内置 ITN 的痛点。

**Architecture:** 新增 `itn.rs` 模块封装 `chinese2digits` crate 的 `take_number_from_string`，插入后处理链 corrector 后、hans 前；流式路径仅在 `finish()`（Final）应用，Partial/Committed 不过。

**Tech Stack:** chinese2digits crate（纯 Rust，无 C 绑定）、octopus-asr-local pipeline

**Spec:** `docs/superpowers/specs/2026-07-27-asr-itn-design.md`

## Global Constraints

- chinese2digits crate 版本 `= "1"`（纯 Rust，无原生依赖）
- ITN 始终应用，无配置开关（ASR 场景）
- Partial/Committed 不过 ITN，仅 Final / 离线 transcription 过
- `force_simplified=true`（繁体数字字符先转简体再识别）
- `pct=false`（不做百分比转换）

---

## File Structure

| 文件 | 操作 | 职责 |
|---|---|---|
| `crates/asr-local/src/itn.rs` | 新建 | ITN 模块：`pub fn normalize(text: &str) -> String` + 内联测试 |
| `crates/asr-local/src/lib.rs:29` | 修改 | 加 `pub mod itn;`（在 `pub mod hans;` 后） |
| `crates/asr-local/src/pipeline.rs:70` | 修改 | transcribe_batch corrector 后、hans 前插 `itn::normalize` |
| `crates/asr-local/src/streaming_runner.rs:280` | 修改 | finish() 的 Final 文本过 `itn::normalize` |
| `crates/asr-local/Cargo.toml` | 修改 | 加 `chinese2digits = "1"` 依赖 |

---

### Task 1: ITN 模块 + 测试（TDD）

**Files:**
- Create: `crates/asr-local/src/itn.rs`
- Modify: `crates/asr-local/src/lib.rs:29`（加 `pub mod itn;`）
- Modify: `crates/asr-local/Cargo.toml`（加依赖）

**Interfaces:**
- Produces: `crate::itn::normalize(text: &str) -> String`

- [x] **Step 1: 加 chinese2digits 依赖**

Modify `crates/asr-local/Cargo.toml`，在 `[dependencies]` 段末尾加：

```toml
chinese2digits = "1"
```

- [x] **Step 2: 在 lib.rs 注册模块**

Modify `crates/asr-local/src/lib.rs`，在 `pub mod hans;`（行 28）后加一行：

```rust
pub mod itn;
```

- [x] **Step 3: 写 itn.rs（含测试 + 实现）**

Create `crates/asr-local/src/itn.rs`：

```rust
//! ASR 数字 ITN（Inverse Text Normalization）：中文数字→阿拉伯数字。
//!
//! 用 chinese2digits crate 的 `take_number_from_string`，从文本中找中文数字
//! 替换为阿拉伯数字，保留非数字文字。解决 Zipformer/Moonshine/Whisper 等
//! 无内置 ITN 引擎输出「二零二六年七月二十六日」而非「2026年7月26日」的问题。
//!
//! 详见 spec 2026-07-27-asr-itn-design.md。

use chinese2digits::take_number_from_string;

/// 中文数字→阿拉伯数字。始终应用，无开关。
///
/// - `force_simplified=true`：繁体数字字符（貳貳參...）先转简体再识别。
///   注意：只管数字字符的繁简，不替代 hans 模块的全文简繁归一。
/// - `pct=false`：不做百分比转换（「百分之五十」不转「50%」，避免误转）。
///
/// 自带 ITN 的引擎（Qwen3/SenseVoice/Paraformer）文本无中文数字 → no-op。
pub fn normalize(text: &str) -> String {
    take_number_from_string(text, false, true).replaced_text
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn year_and_date() {
        assert_eq!(normalize("二零二六年七月二十六日"), "2026年7月26日");
    }

    #[test]
    fn decimal() {
        assert_eq!(normalize("三点五"), "3.5");
    }

    #[test]
    fn count() {
        assert_eq!(normalize("十五个"), "15个");
    }

    #[test]
    fn no_chinese_number_noop() {
        assert_eq!(normalize("今天天气不错"), "今天天气不错");
    }

    #[test]
    fn already_arabic_noop() {
        // Qwen3/SenseVoice 输出已是阿拉伯数字
        assert_eq!(normalize("2026年7月26日"), "2026年7月26日");
    }

    #[test]
    fn english_noop() {
        assert_eq!(normalize("hello world"), "hello world");
    }
}
```

- [x] **Step 4: 运行测试**

Run: `cargo test -p octopus-asr-local itn`
Expected: PASS（全部测试通过；若 chinese2digits 对「二零二六」的输出与预期不完全一致，调整测试断言匹配实际输出，记录差异）

- [x] **Step 5: Commit**

```bash
git add crates/asr-local/src/itn.rs crates/asr-local/src/lib.rs crates/asr-local/Cargo.toml
git commit -m "feat(asr): ITN 模块（chinese2digits 中文数字→阿拉伯）"
```

---

### Task 2: 离线转录 pipeline 接入 ITN

**Files:**
- Modify: `crates/asr-local/src/pipeline.rs:58-76`

**Interfaces:**
- Consumes: `crate::itn::normalize(text: &str) -> String`（Task 1 产出）

- [x] **Step 1: 修改 transcribe_batch 插入 ITN**

Modify `crates/asr-local/src/pipeline.rs` 的 `transcribe_batch` 函数。

当前代码（行 58-76）：
```rust
    let text = if cfg.correct && !engine.skip_corrector() && !is_english {
        let corrected = crate::corrector::get_corrector().correct(&raw_text);
        for word in crate::corrector::drain_hits() {
            if let Err(e) = crate::db::bump_hotword_hit_by_word(&word) {
                log::warn!("[hotword] bump 命中计数失败 '{}': {}", word, e);
            }
        }
        corrected
    } else {
        raw_text
    };

    Ok(if cfg.simplify {
        crate::hans::to_simplified(&text)
    } else {
        crate::hans::to_traditional(&text)
    })
```

改为（在 `let text = ...` 之后、`Ok(if cfg.simplify ...)` 之前插入 ITN）：
```rust
    let text = if cfg.correct && !engine.skip_corrector() && !is_english {
        let corrected = crate::corrector::get_corrector().correct(&raw_text);
        for word in crate::corrector::drain_hits() {
            if let Err(e) = crate::db::bump_hotword_hit_by_word(&word) {
                log::warn!("[hotword] bump 命中计数失败 '{}': {}", word, e);
            }
        }
        corrected
    } else {
        raw_text
    };
    // ITN：中文数字→阿拉伯数字（corrector 后、hans 前，spec 2026-07-27-asr-itn-design §2）
    let text = crate::itn::normalize(&text);

    Ok(if cfg.simplify {
        crate::hans::to_simplified(&text)
    } else {
        crate::hans::to_traditional(&text)
    })
```

- [x] **Step 2: 编译验证**

Run: `cargo build -p octopus-asr-local`
Expected: 0 error

- [x] **Step 3: Commit**

```bash
git add crates/asr-local/src/pipeline.rs
git commit -m "feat(asr): pipeline transcribe_batch 接入 ITN（corrector 后 hans 前）"
```

---

### Task 3: 流式 Final 接入 ITN

**Files:**
- Modify: `crates/asr-local/src/streaming_runner.rs:279-284`

**Interfaces:**
- Consumes: `crate::itn::normalize(text: &str) -> String`（Task 1 产出）

- [x] **Step 1: 修改 finish() 对 Final 文本过 ITN**

Modify `crates/asr-local/src/streaming_runner.rs` 的 `finish` 方法。

当前代码（行 279-284）：
```rust
    pub fn finish(&mut self) -> TranscriptEvent {
        match self.engine.finish() {
            Ok(text) => TranscriptEvent::Final(text),
            Err(e) => TranscriptEvent::Error(e.to_string()),
        }
    }
```

改为：
```rust
    /// 收尾：engine.finish（追加句号 + 简繁归一）→ ITN 数字归一化 → `Final`。
    /// Partial/Committed 不过 ITN（数字未说完可能误转），仅 Final 过。
    pub fn finish(&mut self) -> TranscriptEvent {
        match self.engine.finish() {
            Ok(text) => TranscriptEvent::Final(crate::itn::normalize(&text)),
            Err(e) => TranscriptEvent::Error(e.to_string()),
        }
    }
```

- [x] **Step 2: 编译验证**

Run: `cargo build -p octopus-asr-local`
Expected: 0 error

- [x] **Step 3: 全量测试**

Run: `cargo test -p octopus-asr-local`
Expected: 全部 PASS（含 Task 1 的 ITN 测试 + 现有测试无回归）

- [x] **Step 4: Commit**

```bash
git add crates/asr-local/src/streaming_runner.rs
git commit -m "feat(asr): streaming finish() Final 接入 ITN（Partial/Committed 不过）"
```

---

### Task 4: desktop 集成验证 + 文档同步

**Files:**
- Modify: `docs/architecture.md`（§ASR 后处理链补 ITN）

- [x] **Step 1: desktop 全量编译**

Run: `cargo build -p octopus-desktop --features embedded`
Expected: 0 error（chinese2digits 纯 Rust，无原生依赖问题）

- [x] **Step 2: desktop 测试无回归**

Run: `cargo test -p octopus-desktop`
Expected: 全部 PASS

- [x] **Step 3: 更新 architecture.md**

在 architecture.md 的 ASR 后处理链描述处（搜索 `corrector` 或 `hans::to_simplified`），补 ITN 步骤说明：

> 引擎输出经 `corrector`（拼音纠错）→ `itn::normalize`（🆕 中文数字→阿拉伯，chinese2digits crate，corrector 后 hans 前）→ `hans`（简繁归一）三步后处理。流式仅 Final 过 ITN（Partial/Committed 避免「二」→「2」未说完误转）。

- [x] **Step 4: Commit**

```bash
git add docs/architecture.md
git commit -m "docs: architecture.md 补 ASR ITN 后处理步骤"
```
