- [x] 热词的更新失败，添加了很多热词，但同步的时候会被仓库里面的数据覆盖。
  - **已修复**（2026-08-01，分支 `bugfix/pr-0801`）：热词 pull/push 升级到 `merge_hotwords`（3-way merge，对称 vault `merge_vault`）。根因：旧 `pull_hotwords_from_files` 无方向感知，本地加词后 `sync_md5` 变，与旧 outline md5 不等 → pull 用旧文件覆盖 DB → 新词丢失。详见 [spec](superpowers/specs/2026-08-01-hotword-sync-merge-model.md) + [plan](superpowers/plans/2026-08-01-hotword-sync-merge-model.md)。
  - **遗留**：热词软删 tombstone（跨设备删除复活）记入 spec §5 已知问题，未修。
- [x] 热词好像没有生效，我听了比较多的热词，但好像没有一个命中的记录。
  - **已修复**（2026-08-01，分支 `bugfix/pr-0801`）：流式听写路径激活热词纠错——`from_session` 传 `correct = asr_correct && language != en`；`StreamingRunner.finish()` 加 corrector 注入（Partial/Committed/finish 全过）；finish 后 `drain_hits`+`bump_hotword_hit_by_word` 命中入库；`asr_correct` 默认改 `true`。详见 [spec](superpowers/specs/2026-08-01-hotword-streaming-effective.md) + [plan](superpowers/plans/2026-08-01-hotword-streaming-effective.md)。
  - **遗留**：cloud 流式路径的热词纠错未处理（需确认服务端/客户端分工）；存量库 `asr_correct` 不强制迁移（老用户手动开）。
- [x] instant 模式的语音识别，需要把识别到的文本即时显示到指示窗口，而不是一直一个正在聆听，最后给一个结果。
  - **已修复**（2026-08-01，分支 `bugfix/pr-0801`）：前端 `update-result` handler 在 `recordMode === "instant"` 时也写 `instantText`（读 `recordModeRef` 避免闭包陷阱）；InstantView listening 态显示尾部最新内容（28 字符）。详见 [spec](superpowers/specs/2026-08-01-instant-live-text.md) + [plan](superpowers/plans/2026-08-01-instant-live-text.md)。
