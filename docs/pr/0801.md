- [x] 热词的更新失败，添加了很多热词，但同步的时候会被仓库里面的数据覆盖。
  - **已修复**（2026-08-01，分支 `bugfix/pr-0801`）：热词 pull/push 升级到 `merge_hotwords`（3-way merge，对称 vault `merge_vault`）。根因：旧 `pull_hotwords_from_files` 无方向感知，本地加词后 `sync_md5` 变，与旧 outline md5 不等 → pull 用旧文件覆盖 DB → 新词丢失。详见 [spec](superpowers/specs/2026-08-01-hotword-sync-merge-model.md) + [plan](superpowers/plans/2026-08-01-hotword-sync-merge-model.md)。
  - **遗留**：热词软删 tombstone（跨设备删除复活）记入 spec §5 已知问题，未修。
- [ ] 热词好像没有生效，我听了比较多的热词，但好像没有一个命中的记录。
  - **已诊断**（2026-08-01）：流式听写路径硬编码 `correct=false`，热词索引建好但永不执行；流式 `finish()` 也不调 correct()；流式路径无 `drain_hits`/`bump_hotword_hit` 命中不入库。待修。
- [ ] instant 模式的语音识别，需要把识别到的文本即时显示到指示窗口，而不是一直一个正在聆听，最后给一个结果。
  - **已诊断**（2026-08-01）：前端事件路由漏接——流式 partial 走 `update-result` 事件，但 `update-result` handler 只写 toggle 视图 `text`，不写 `instantText`；instant 视图只订阅 `instant-state`（recording 期间只 emit 一次空文本）。待修。
