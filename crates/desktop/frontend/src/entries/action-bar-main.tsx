// ActionBar 窗口（悬浮命令面板）入口。依赖闭包：lucide-react + 内部 search/urlDetect 簇。
import "@/index.css";
import { mountApp } from "@/lib/mountApp";
import ActionBar from "@/pages/ActionBar";

mountApp(<ActionBar />);
