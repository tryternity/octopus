[package]
name = "octopus-egui"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "octopus-egui"
path = "src/main.rs"

# Task 4 骨架：仅验证 eframe 核心可编。
# octopus-infra/notepad（IPC+store）Task 5/6 按需引入；egui_commonmark（md 预览）Task 6 引入并做版本配对。
[dependencies]
eframe = "0.29"
egui = "0.29"
